# Tests

`node tests/jd_engine.test.js` — exercises the built-in job-description / offer-letter engine
(extracted live from index.html) across every role x document kind x language x custom-terms
combination and asserts structure, labor-law rules, bilingual integrity and required papers.
Run it after any change to the engine or the job roles. Sample outputs land in tests/fixtures/.
